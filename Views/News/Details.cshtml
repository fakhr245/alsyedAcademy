@model alsyedAcademy.Models.News

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="probootstrap-section probootstrap-section-colored">
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-left section-heading probootstrap-animate mb0">
                <h1 class="mb0">@Model.title</h1>
            </div>
        </div>
    </div>
</section>

<section class="probootstrap-section probootstrap-bg-white">
    <div class="container">       
        <div class="row">      
                <div class="col-md-12">                  
                    <div>
                        <h8>@Model.dated - @Model.description</h8>
                        <p>
@if (User.IsInRole("admin") == true)
{
      Html.ActionLink("Edit", "Edit", new { id = Model.id });
}
                            @Html.ActionLink("Back to List", "Index")
                        </p>
                        <img src="../../images/newsoriginal/@Model.photo" alt="@Model.description" class="img-responsive">
                    </div><p>
  
</p>
                </div>
               
            </div>
        </div>
    </section>
                <style>
                    #outPopUp {
                        position: absolute;
                        width: 300px;
                        height: 200px;
                        z-index: 15;
                        left: 50%;
                        margin: -100px 0 0 -150px;
                        background: red;
                    }
                </style>
